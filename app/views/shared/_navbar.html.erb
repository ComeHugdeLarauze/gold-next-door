<div class="navbar-wagon">
  <!-- Logo -->
  <div>
  <%= link_to crews_path, class: "navbar-crew" do %>
    <%= image_tag "treasure-map.svg", height: 50 %>
    <p class="crew-navbar">Mes Crews</p>
  <% end %>
  </div>

  <div>
  <%= link_to tresors_path  do %>
  <%= image_tag "logo-gnd.png", class: "navbar-logo" %>
  <% end %>
  </div>


  <!-- Right Navigation -->
  <div class="navbar-wagon-right hidden-xs hidden-sm">
    <% if pirate_signed_in? %>
      <!-- Avatar with dropdown menu -->
        <div class="navbar-wagon-item">
        </div>
    <% else %>
      <!-- Login link (when logged out) -->
      <%= link_to t(".sign_in", default: "Login"), new_pirate_session_path, class: "navbar-wagon-item navbar-wagon-link" %>
    <% end %>
  </div>

  <!-- Dropdown list appearing on mobile only -->
  <div class="navbar-wagon-item hidden-md hidden-lg">
    <div class="dropdown">

      <% if pirate_signed_in? && @current_pirate.photo? %>
        <%= cl_image_tag @current_pirate.photo, class: 'avatar', height: 50, data: { toggle: "dropdown" } %>
      <% else %>
        <%= image_tag 'pirate.svg', class: 'avatar', height: 50, data: { toggle: "dropdown" } %>
      <% end %>
      <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
        <% if pirate_signed_in? %>
      <%= cl_image_tag @current_pirate.photo, class: 'avatar', height: 50, data: { toggle: "dropdown" } %>
          <%= link_to destroy_pirate_session_path, method: :delete do %>
            <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Log out") %>
          <% end %>
        <% else %>
          <li>
            <%= link_to t(".sign_in", default: "Login"), new_pirate_session_path, class: "navbar-wagon-item navbar-wagon-link" %>
          </li>
        <% end %>
        <%= link_to 'Profile', profile_path, class: "navbar-wagon-item navbar-wagon-link"  %>
      </ul>
    </div>
  </div>
</div>

