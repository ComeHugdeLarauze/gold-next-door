<% crews.each do |crew| %>
  <%= link_to crew_path(crew), class: 'no-text-decoration' do %>
  <div class="card-crew">
    <h2 class="h2-crew-card"><%= crew.nice_nom %>
      <% if crew.prive %>
      <%= image_tag "ferme.svg", height: 20, width: 20, crop: :fill%>
      <% end %>
    </h2>
    <div class="crew-compteurs">
      <p><%= pluralize crew.pirates.count, "pirate" %></p>
      <p><%= pluralize crew.tresors.count, "trésor" %></p>
    </div>
  </div>
  <% end %>
<% end %>
