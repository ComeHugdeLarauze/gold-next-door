<div class="container-tresor">
  <h2>Alors, on a oublié son mot de passe, Captain ?</h2>

  <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :email, label: 'Ton email', required: true, autofocus: true %>
    </div>

    <div class="wide_buttons">
      <%= f.button :submit, "M'envoyer les instructions 🔑", class: 'button-2 yellow' %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
